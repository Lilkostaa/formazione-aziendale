/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        protocol: 'https',
        hostname: '**',
      },
    ],
  },
  serverActions: {
    bodySizeLimit: '10mb',
  },
}

export default nextConfig
